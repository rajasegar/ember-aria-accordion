{{yield (hash
  panel=(component 'accordion-panel'
  active=this.active
  toggle=@toggle
  multiple=@multiple
  refresh=@refresh
  select=this.selectAccordion
  focusAccordion=this.focusAccordion
  ))  }}
