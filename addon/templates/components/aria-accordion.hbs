{{yield (hash
  panel=(component 'accordion-panel'
  active=this.active
  toggle=@toggle
  multiple=@multiple
  refresh=@refresh
  activePanels=this.activePanels
  select=this.selectAccordion
  focusAccordion=this.focusAccordion
  ))  }}
