{{yield (hash
  header=(component 'aria-accordion/header' on-click=(action 'toggleIsOpen') isOpen=isOpen)
  panel=(component 'aria-accordion/panel' shouldAnimate=shouldAnimate isOpen=isOpen)
  name=name
  isOpen=isOpen
)}}
